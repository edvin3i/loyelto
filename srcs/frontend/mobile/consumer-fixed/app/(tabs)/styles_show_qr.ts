import { StyleSheet } from 'react-native';

const styles_show_qr = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
});

export default styles_show_qr; 