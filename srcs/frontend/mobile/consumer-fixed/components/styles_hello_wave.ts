import { StyleSheet } from 'react-native';

const styles_hello_wave = StyleSheet.create({
  text: {
    fontSize: 28,
    lineHeight: 32,
    marginTop: -6,
  },
});

export default styles_hello_wave; 